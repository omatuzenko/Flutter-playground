name: playground
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  bloc: ^8.1.0
  flutter:
    sdk: flutter

  # UI
  cached_network_image: ^3.3.0
  flutter_seekbar: 1.0.1
  cupertino_icons: ^1.0.2

  # Network
  http: 1.1.0

  # Database
  sqflite: ^2.3.0

  # Util
  flutter_bloc: 8.1.3
  rxdart: ^0.27.7
  # cloud_firestore: ^4.9.3 # https://firebase.google.com/docs/firestore/quickstart
  intl: ^0.18.1

dev_dependencies:
  bloc_test: ^9.1.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.1

  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/images/projects/booksviewer/

    - assets/images/onboarding/
    - assets/images/viewpagerparallax/
    - assets/images/musicplayer/

  uses-material-design: true
